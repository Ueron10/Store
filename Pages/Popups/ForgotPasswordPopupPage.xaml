<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="StoreProgram.Pages.Popups.ForgotPasswordPopupPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Lupa Password"
             BackgroundColor="#80000000"
             Padding="24">

    <Grid RowDefinitions="*" ColumnDefinitions="*">
        <Frame Padding="18"
               BackgroundColor="{StaticResource CardBackground}"
               CornerRadius="16"
               HasShadow="True"
               VerticalOptions="Center"
               HorizontalOptions="Center"
               WidthRequest="380">

            <VerticalStackLayout Spacing="12">
                <Label Text="Lupa Password"
                       FontSize="18"
                       FontAttributes="Bold"
                       TextColor="{StaticResource TextPrimary}" />

                <Label Text="Masukkan username dan email. Jika cocok, password akan ditampilkan."
                       FontSize="12"
                       TextColor="{StaticResource TextPrimary}" />

                <VerticalStackLayout Spacing="6">
                    <Label Text="Username" FontAttributes="Bold" TextColor="{StaticResource Gray900}" />
                    <Frame BackgroundColor="{StaticResource Gray100}" CornerRadius="10" Padding="0" HasShadow="False">
                        <Entry x:Name="UsernameEntry" Placeholder="Masukkan username" BackgroundColor="Transparent" />
                    </Frame>
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="6">
                    <Label Text="Email" FontAttributes="Bold" TextColor="{StaticResource Gray900}" />
                    <Frame BackgroundColor="{StaticResource Gray100}" CornerRadius="10" Padding="0" HasShadow="False">
                        <Entry x:Name="EmailEntry" Placeholder="Masukkan email" Keyboard="Email" BackgroundColor="Transparent" />
                    </Frame>
                </VerticalStackLayout>

                <Label x:Name="ErrorLabel"
                       TextColor="{StaticResource Danger}"
                       FontSize="12"
                       IsVisible="False" />

                <Frame x:Name="PasswordBox"
                       BackgroundColor="{StaticResource Gray100}"
                       CornerRadius="12"
                       Padding="12"
                       HasShadow="False"
                       IsVisible="False">
                    <VerticalStackLayout Spacing="6">
                        <Label Text="Password Anda:" FontAttributes="Bold" TextColor="{StaticResource Gray900}" />
                        <Label x:Name="PasswordLabel" FontSize="16" TextColor="{StaticResource Gray900}" />
                    </VerticalStackLayout>
                </Frame>

                <Grid ColumnDefinitions="*,*" ColumnSpacing="10" Margin="0,6,0,0">
                    <Button Grid.Column="0"
                            Text="Cancel"
                            BackgroundColor="{StaticResource Gray200}"
                            TextColor="{StaticResource Gray900}"
                            CornerRadius="10"
                            HeightRequest="44"
                            Clicked="OnCancelClicked" />

                    <Button x:Name="ConfirmButton"
                            Grid.Column="1"
                            Text="Konfirmasi"
                            BackgroundColor="{StaticResource Primary}"
                            TextColor="{StaticResource TextPrimary}"
                            CornerRadius="10"
                            HeightRequest="44"
                            Clicked="OnConfirmClicked" />
                </Grid>
            </VerticalStackLayout>

        </Frame>
    </Grid>
</ContentPage>
